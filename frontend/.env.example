# Sao chép thành .env (local) hoặc set trong Vercel -> Settings -> Environment Variables.
# URL gốc của backend, PHẢI có /api ở cuối.
# Local:
VITE_API_BASE_URL=http://localhost:8080/api
# Production (ví dụ): https://distribution-backend.onrender.com/api
